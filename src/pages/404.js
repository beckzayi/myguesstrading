import React from 'react';
import Layout from '../components/layout';

const NotFound = () => (
  <Layout>
    <div>
      <h1>Page not found</h1>
      <div className="mt-4">
        <p>The page never existed.</p>
      </div>
    </div>
  </Layout>
);

export default NotFound;
